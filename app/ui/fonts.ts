import { Inter, Lusitana,Anton,Kanit, Freehand } from 'next/font/google';
 
export const inter = Inter({ subsets: ['latin'] });
export const lusitana= Lusitana({
    weight: ['400', '700'],
    subsets: ['latin'],
  });

  export const kanit = Kanit({
    weight: ['400'],
    subsets: ['latin'],
  });

  export const anton = Anton({
    weight: ['400'],
    subsets: ['latin'],
  });
  export const freehand=Freehand({
    weight: ['400'],
    subsets: ['latin'],
  });



